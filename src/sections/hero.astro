<!-- eslint-disable -->
<section class="hero reveal" id="hero" data-nav-title="Axiom Intelligence">
  <div class="container">
    <span class="label">AXIOM INTELLIGENCE</span>
    <h1>A Self-Evolving Superintelligence Engine for Autonomous Research Organisms.</h1>
    <p>AI is currently passive. We are building the infrastructure for the next era: AI that independently researches, executes, and evolves in the real world.</p>
  </div>
</section>

<style>
  .hero::before {
      content: ''; position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      width: 1000px; height: 1000px; background: radial-gradient(circle, var(--glow-color) 0%, transparent 70%);
      pointer-events: none; z-index: 1; animation: hero-pulse 8s ease-in-out infinite;
  }

  @keyframes hero-pulse {
      0% { opacity: 0.4; transform: translate(-50%, -50%) scale(1); }
      50% { opacity: 0.8; transform: translate(-50%, -50%) scale(1.1); }
      100% { opacity: 0.4; transform: translate(-50%, -50%) scale(1); }
  }

  .hero h1 {
      background: linear-gradient(90deg, #FFFFFF 0%, #888888 50%, #FFFFFF 100%);
      background-size: 200% auto; -webkit-background-clip: text; background-clip: text;
      -webkit-text-fill-color: transparent; animation: text-shimmer 8s linear infinite;
  }

  @keyframes text-shimmer { to { background-position: 200% center; } }

  h1 { font-size: clamp(2.8rem, 7vw, 5.5rem); letter-spacing: -0.03em; margin-bottom: 40px; max-width: 1000px; }

  @media (max-width: 768px) {
      .hero { min-height: 100vh; }
  }
</style>
