<section class="deployment reveal" id="deployment" data-nav-title="Current Deployment">
  <div class="container">
    <h2 class="label">CURRENT DEPLOYMENT</h2>
    <div class="bento-grid deployment-grid">
      <article class="bento-item">
        <h3 class="bento-title">Foundational Service</h3>
        <p class="bento-text">To train and monetize the core engine, we deployed it in a constrained, high-margin B2B environment. Acting as "McKinsey for Main Street," this autonomous consultant proves safe AI execution in the real world, operating at ~98% gross margins to self-fund our broader R&D vision.</p>
      </article>
    </div>
  </div>
</section>

<style>
  .deployment {
      background-image: linear-gradient(var(--grid-line) 1px, transparent 1px);
      background-size: 100px 100px; animation: pan-down 30s linear infinite;
  }

  @keyframes pan-down { 0% { background-position: 0 0; } 100% { background-position: 0 100px; } }

  .deployment-grid { grid-template-columns: 1fr; max-width: 800px; margin-left: auto; margin-right: auto; }

  .deployment-grid .bento-item { animation: card-breathe 6s ease-in-out infinite; }

  @keyframes card-breathe {
      0%, 100% { box-shadow: 0 20px 40px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.1), 0 0 0px rgba(40, 80, 120, 0); }
      50% { box-shadow: 0 20px 40px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.1), 0 0 40px rgba(40, 80, 120, 0.1); }
  }

  .deployment-grid .bento-item { align-items: center; text-align: center; }
  .deployment-grid .bento-text { text-align: center; }
</style>
